<%= form_with(model: event_response, local: true) do |form| %>
  <% if event_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_response.errors.count, "error") %> prohibited this event_response from being saved:</h2>

      <ul>
      <% event_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
